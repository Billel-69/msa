# Test environment configuration
NODE_ENV=test
JWT_SECRET=test_secret_key_for_jenkins_ci_cd

# Test Database Configuration (SQLite for CI/CD simplicity)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=msa_test
DB_PORT=3306

# Test-specific settings
BCRYPT_ROUNDS=1
JWT_EXPIRES_IN=1h

# Disable external services in tests
OPENAI_API_KEY=test_key_disabled
ASSISTANT_ID=test_assistant_disabled

# Upload settings for tests
MAX_FILE_SIZE=10
UPLOAD_PATH=/tmp/test_uploads

# Rate limiting (more permissive for tests)
RATE_LIMIT_REQUESTS=1000
RATE_LIMIT_WINDOW=60000

# CORS for testing
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000

# Debug mode for tests
DEBUG=false
LOG_LEVEL=error